# Golden Cases

The golden files are not a benchmark. They are a stable review surface for reliability.

The main golden fixture is `fixtures/golden/scoreboard.csv`. The matrix fixture is `fixtures/golden/lane-matrix.csv`. Together they cover `budget pressure`, `failure width`, `recovery gap`, and `runbook drift` with different score ranges.

The purpose is to make intentional rule changes show up in review.
